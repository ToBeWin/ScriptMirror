# ScriptMirror 真机验收清单

这份清单用于最后一轮 Android / iOS 真机验收。模拟器已经覆盖了启动、权限拒绝、录制保存、切后台中断恢复、重命名持久化和旋转元数据；相机、麦克风、Photos/相册、前置镜像和离线 ASR 仍需要在真实设备上确认。

当前自动化证据（2026-08-31）：API 36 模拟器前置录制已生成可播放的 H.264 1280×720 + 16 kHz 单声道 AAC，竖屏文件保留 `90°/-90°` Display Matrix；成片 `VideoCapture` 现在按“自拍镜像”设置与前置预览同步（开启时 `MIRROR_MODE_ON_FRONT_ONLY`，关闭时 `MIRROR_MODE_OFF`）；预览与视频同时绑定同一个 `ViewPort`，用于保证满屏取景和成片裁剪同源。已确认 AAC 配置帧不会被误写入成片，跨轨道重复时间戳会被规范化，`ffprobe` 可读出完整轨道与时长；最新 Release 包完成了约 30 秒短录制和一次约 10 分 08 秒的连续录制（`607.680` 秒），两次完成页均显示“录制完成”，长录制文件包含 H.264 + AAC 轨道且没有 Fatal/PlatformException；并在“停止后立即退出页面”压力路径中保留了可播放的短视频和 AAC 轨道，没有产生空 pending 行；虚拟摄像头的 `ffmpeg` null-muxer 检查可能出现时间戳警告，不作为真机成片失败依据；针对 CameraX 编码器错误码 8，完成页会如实显示“录制未保存”，且不会在 `DCIM/ScriptMirror` 留下空的 MediaStore 条目；API 28 arm64 模拟器的虚拟相机在系统 `media.codec` 中会触发 `Codec2BufferUtils` 崩溃，最新包已确认显示“录制已中断 / 相机编码器未能生成视频”，且没有留下空文件，因此该模拟器只能验证错误态和旧存储回退，不能作为真实设备录制通过的依据；当前主机是 arm64，API 26 AVD 使用 x86 系统镜像，无法在本机 QEMU2 启动，需在 x86 主机或真实 API 26 设备补测；随包模型和词表的 SHA-256 校验路径已通过构建验证；主机端同款 sherpa-onnx 模型烟测已输出中文 partial，组合烟测在慢速、正常、快速三组本地中文口播样本上均验证两次单调推进且无回退；本轮清空应用数据后分别拒绝相机和麦克风权限，应用均回到拍摄准备页并明确提示权限未开启，没有卡在加载状态；随后重新授予权限，短录制再次完成并生成 H.264+AAC 文件，MediaStore 中没有 `is_pending=1` 残留。以上都不能替代真实手机的镜头、麦克风和 ASR 准确率验收。

最终交付 APK 在 API 36 模拟器从干净数据开始独立安装回归：授权后进入“跟稿中”，停止后完成页显示“录制完成”；最新构建又生成了 6.848 秒可播放文件，`ffprobe` 检查包含 H.264 视频和 AAC 音频，MediaStore 没有 pending 行，应用日志没有 Fatal/PlatformException。另一次将相机和麦克风标记为永久拒绝后，点击提示条“打开设置”已实际跳转到系统应用详情页。

## 本轮追加验证（2026-08-31，API 33 Pixel Tablet 模拟器）

- 当前 Release `0.1.1+2008` 重新安装后，首次授权、拍摄准备、录制、完成页和“打开已保存视频”均可走通；系统相册播放器实际被拉起，日志没有 Fatal/PlatformException。
- 录制中将显示方向从竖屏切到横屏后再停止，完成页分别报告 `720×1280` 和 `1280×720`；最新 MediaStore 行均为 `is_pending=0`，路径为 `DCIM/ScriptMirror`，文件包含 H.264 视频和 AAC 音频。
- 录制中按 Home 触发宿主中断，回到应用后自动进入“录制完成”，显示“录制被中断，但视频已保存”，并保留到第 3 行；生成的媒体行仍可读取，没有空条目。
- 手动提词模式在识别不可用时显示“手动翻句”，不会误报“按节奏提词”；异常设置值归一化和离线 ASR/对齐烟测也已通过。
- 在同一 API 36 模拟器切换到后置镜头后完成短录制；CameraX 报告 `Facing: Back`，完成页显示“录制完成”，新媒体行仍为 `is_pending=0`，并包含 H.264 + AAC 轨道。
- 从 API 36 启动器应用抽屉确认应用图标显示为极简镜框 Logo，冷启动过渡也使用同一 Logo。
- 示例文稿不写入用户文稿库，但恢复记录使用稳定 ID；进程被系统杀死后重新进入首页时，可重建示例文稿并继续恢复，不会误清掉有效 checkpoint。
- 首页“开始拍摄”现在也使用同一默认示例文稿，因此从主入口开始的首次录制同样具备中断恢复能力；停止后的音视频合并期间，录制页会锁定提词与设置操作并显示保存进度。
- API 36 清空应用数据后的端到端验证：从首页主按钮进入默认示例、录制并强制结束进程，冷启动后首页实际显示“发现未完成录制 / 夏季防晒分享 · 从第 2 行继续”；启动日志无 Fatal/PlatformException。
- 新建文稿页输入标题或台词后，顶部返回和系统返回都会先显示“放弃这篇文稿？”确认，避免长文稿被误触丢失。

## 安装

1. 普通手机优先使用 `build/app/outputs/flutter-apk/app-release.apk`；确认设备为 arm64 时也可使用更小的 `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`。Release 构建已启用 R8/ProGuard 与资源压缩；当前四个 APK 使用相同 versionCode，可互相覆盖安装。
2. 首次启动时允许相机和麦克风权限；Android 9 及以下按系统提示允许媒体写入，应用优先写入 `DCIM/ScriptMirror`，旧存储提供者不允许建目录时会回退到 MediaStore 标准共享视频位置。
   每次进入录制准备时，应用只会清理自己在 `DCIM/ScriptMirror` 中留下且超过 1 小时的未完成 pending 媒体行，以及同目录中匹配自身命名规则的孤儿 `.pending-…ScriptMirror_*.mp4` 临时文件，用于回收进程异常退出后的残留空间，不会触碰其他相册视频。
3. 可在无网络（飞行模式）下完成整套测试，应用不需要联网。

### iOS 补充

1. 在 Xcode 打开 `ios/Runner.xcworkspace`，将 Team、Bundle ID 和签名配置替换为自己的开发者账号；模拟器只能验证 UI、权限流程和错误态，不能替代真实摄像头录制。
2. 使用 iOS 15 或更高版本的真实 iPhone/iPad，首次启动允许相机、麦克风和 Photos 添加权限；视频应出现在系统“照片”中，而不是应用私有目录。
3. 依次验证前置/后置镜头、720p/1080p、自拍镜像开关和系统返回/切后台。iOS 原生层使用 AVFoundation 跟随界面方向更新预览与成片旋转，前置预览与成片镜像策略必须一致。
4. 开启飞行模式朗读中文文稿，确认随包 sherpa-onnx 中文模型仍能显示“跟稿中”；再用英文文稿重复一次，确认设置为“自动（按文稿）”时加载英文模型并正常推进。若设备硬件采样率不是 16 kHz，原生层会先转换为 16 kHz 单声道再交给离线识别。

## 录制与相册

1. 新建一篇 5–10 句中文文稿，进入拍摄页。
2. 分别测试前置和后置镜头，画质各测试 720p、1080p。
3. 录制 20–30 秒后停止，确认完成页显示真实时长和分辨率。
4. 在系统相册检查 `DCIM/ScriptMirror`：视频可播放、有声音、画面方向正确，且没有横向/倒置。
5. 前置镜头分别测试“自拍镜像”打开与关闭：打开时拍摄页预览和保存视频应保持完全一致的镜像自拍感；关闭时预览和成片均为正常方向。重点观察脸部左右特征、发缝、单侧耳饰或单手动作在回放时是否与录制时一致；文字方向不是本项验收标准。
6. 在录制中将手机从竖屏旋转到横屏（或反向），确认画面不歪、不拉伸，停止后仍可播放。
7. 在倒计时结束、相机仍在准备的瞬间点击“取消/停止”，确认页面停在取消提示，不会随后偷偷开始一段录制；如果 CameraX 已经跨过启动边界，用户取消仍应由原生层停止并删除这段刚启动的片段。
8. 在拍摄准备页快速连续点击“开始录制”，确认只出现一次权限请求和一个录制页，按钮会先显示“正在准备…”。
9. 录制页按系统返回键：录制中应先出现“结束并保存”确认，倒计时阶段应出现“离开”确认；确认后不能留下后台录制。

## 文稿编辑

1. 在任意台词右侧打开编辑面板，修改内容、预计时长和句后停顿后保存。
2. 使用“拆分这句”确认带标点的长句会按自然停顿拆成多行；没有标点时应按中间位置拆分。
3. 使用“合并下一句”确认相邻台词合并后时长相加、停顿沿用后一行，并重新进入拍摄准备页仍能看到合并结果。
4. 修改台词或节奏后直接按顶部返回/系统返回，确认会出现保存提示；选择“保存并返回”后重新打开文稿，改动仍在。

## 离线 ASR

1. 开启飞行模式，重新进入拍摄页并开始录制。
2. 倒计时前应看到“正在准备本地识别”；进入录制后状态应显示“跟稿中”。
3. 用正常、慢速、快速各朗读一遍文稿，观察台词是否按顺序自动推进；手动下一句仍应立即可用。
4. 在安静环境连续读约 20 句，记录漏识别、误推进和延迟。
5. 关闭麦克风权限或制造识别失败时，录制不能中断，状态应降级为“按节奏提词”，定时/手动模式仍可完成。

## 中断与权限

- 录制中按 Home、锁屏或接听来电，恢复后应明确提示“录制被中断，但视频已保存”（若系统确实保存），并保留可继续的台词位置。
- 如果系统在“相机启动交接窗口”把应用切到后台，应保留已开始的片段并进入同一中断恢复路径；这和用户主动点击取消的丢弃语义不同。
- 拒绝相机或麦克风权限后重新点击开始，应看到解释和下一步，而不是卡在加载页；若系统已记住拒绝，提示条上的“打开设置”应直接进入应用详情页，重新允许后可以再次录制。
- 低存储、相机被其他应用占用时，失败提示必须说明视频是否保存以及下一步操作。
- 长时间录制或人为制造高温状态时，录制页出现温度提示但不会强制中断；完成后下一次可切换 720p。

## 记录结果

请记录：手机型号、Android 版本、前/后置镜头、分辨率、镜像开关、是否飞行模式、ASR 是否自动推进、最终视频是否在相册以及任何错误提示。真机验收完成后，再决定是否调整默认镜像策略或 ASR 阈值。
