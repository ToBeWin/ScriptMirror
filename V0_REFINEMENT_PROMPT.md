# ScriptMirror / 镜词 — v0 精修提示词

请把下面整段作为一个完整 prompt 使用。将 `stitch_scriptmirror_app_design_ui` 里的 Stitch 截图，以及项目内的 `ScriptMirror_UI_Refined.pen` Pen.dev 高保真稿作为视觉参考一并提供给你。

```text
你是一名资深移动端产品设计师、交互设计师和 Flutter design-system engineer。请基于我提供的 Stitch 视觉稿，为 Android-first 手机应用「镜词 / ScriptMirror」做高保真 UI/UX 精修。产品是一款本地优先的自拍视频提词器：用户粘贴文稿，系统分成台词行；录制自拍视频时显示当前句和后续 2–3 句；用户可以定时或手动翻句，ASR 只作为可选的后台辅助信号。

一、绝对不能改变的产品边界

1. 不要加入登录、注册、账号、订阅、广告、社交、云同步或强制联网。
2. 不要把产品做成聊天机器人、剪辑器、滤镜相机或内容社区。
3. Android CameraX 是录制期间相机、麦克风和最终视频的唯一原生所有者；UI 只调用 CaptureService 契约，不直接调用平台 API。
4. SQLite 文稿 repository、ScriptParser、AlignmentEngine、AsrProvider 契约已经存在；UI 改版必须保留这些数据和接口。
5. ASR 不可用时必须明确显示“按节奏/手动提词”降级状态，录制仍然可用；不得把模拟识别结果显示成真实识别。
6. 录制失败或视频未保存时，完成页必须显示“录制未保存”和真实错误，不得显示成功图标、虚构时长或虚构文件大小。
7. 不要改变核心流程：创建/选择文稿 → 编辑台词 → 拍摄准备 → 录制 → 完成/重录。

二、视觉方向

- 延续 Stitch 的 Obsidian Cyber / premium dark 方向，但降低赛博噪音，优先可读性和长时间拍摄舒适度。
- 背景 #0B0C0E，内容面板 #1B1E23，边框 #31363F，正文 #F5F6F7，次要文字 #BEC4CC。
- Cyan #16DDE0 只用于主要动作、选中态、可用状态；Recording Red #FF4D4F 只用于真正的录制/停止危险动作；Amber #FFB454 用于降级、警告和未保存。
- 移动端画布优先 390×844，左右安全边距 20–24px，所有可点击控件最小 48×48px。
- 使用 Inter/系统无衬线字体；台词阅读优先，正文字号 18–32px，行高 1.35–1.5；不要用发光、渐变、阴影干扰台词。
- 卡片使用轻微玻璃质感和 12–16px 圆角；不要大面积霓虹描边，不要把所有元素都做成胶囊。
- 保持明确的层级：标题 > 当前台词 > 后续台词 > 元数据；控制项少而清楚。

三、需要精修的页面和交互

1. 首页 Home
   - 顶部显示“镜词”和一句简短副标题，右上角是设置。
   - 中央是唯一最突出的“开始拍摄”主动作，下面是最近文稿。
   - 文稿卡片显示标题、行数、预计时长、最近更新时间；点击卡片进入同一篇文稿编辑。
   - “新建文稿”打开标题和整篇文本粘贴入口；空文本要有明确错误提示。
   - 没有已保存文稿时可以显示示例卡片，但必须明显标注“示例文稿”，不能伪装成用户数据。

2. 文稿编辑 Script Editor
   - 顶部显示标题、预计总时长、台词行数和删除入口（仅真实保存的文稿可删除）。
   - 台词以可拖拽排序的卡片呈现，每行显示序号、文本、预计时长、句后停顿和编辑入口。
   - 编辑台词使用底部 sheet；保存后立即更新预览。
   - 重点优化长文稿滚动、键盘避让、拖拽反馈、空状态和删除确认。
   - 主按钮是“进入拍摄准备”，不要在这里引入复杂设置。

3. 拍摄准备 Preparation
   - 显示当前文稿摘要、前置/后置镜头切换、画质（默认 1080p，可选 720p）、提词速度/自动推进和语音跟稿开关。
   - 首次点击开始录制时请求相机和麦克风权限，解释文案必须是“用于录制自拍视频”和“用于保存视频声音与智能跟稿”。
   - 如果 ASR 不可用，保留可用的定时/手动提词，并用非阻塞提示说明原因。
   - 开始录制按钮必须是清晰、稳定、足够大的主操作；不要让用户在准备页寻找入口。

4. 沉浸式录制 Recording
   - 相机画面为底层，台词面板位于上半部或安全区域，不能遮挡主要人脸区域。
   - 当前句最大、最高对比度；后续 2–3 句逐级减弱；已读内容消失。
   - 顶部显示真实录制状态和计时；状态至少区分“跟稿中”“正在确认”“按节奏提词”。
   - 底部固定上一句、红色停止、下一句三个控制；触控目标至少 56px。
   - 自动推进和手动翻句都必须可用；手动操作后给出短暂冻结反馈，不要立刻被后台识别覆盖。
   - 录制中不展示 ASR 原始全文，不展示 token、网络日志或调试信息。

5. 完成 Complete
   - 成功：显示“录制完成”“视频已保存到相册”、真实时长和可验证的媒体信息。
   - 失败/预览模式：显示“录制未保存”、Amber 错误状态、真实错误文案和下一步建议；不使用成功勾号。
   - 按钮顺序：完成返回首页、再录一次、继续拍同一文稿；后两个必须保留当前文稿。
   - 不要硬编码 01:36、184 MB 等示例数据；没有数据就显示“—”。

6. 设置 Settings
   - 设置字号、背景透明度、前瞻行数、镜像预览、默认画质和存储位置；以简洁列表/底部 sheet 呈现。
   - 每项要有当前值、可点击反馈和清晰的恢复默认路径。
   - 不加入账号、隐私追踪、在线服务开关以外的复杂设置。

四、状态与无障碍

- 设计 loading、空状态、权限拒绝、相机不可用、ASR 不可用、保存失败、保存成功、长文本和小屏幕状态。
- 错误信息必须回答三件事：视频是否已保存、台词位置是否保留、用户下一步能做什么。
- 所有颜色状态同时配合文字/图标，不只依赖颜色；保证暗色背景上的对比度。
- 不能因为 ASR、网络或权限异常让录制页崩溃或卡死。

五、交付格式

请先输出：
1. 一份页面信息架构和用户流程；
2. 一份可复用的设计 token（颜色、字号、间距、圆角、控件高度）；
3. 六个页面的高保真移动端实现/原型；
4. 每个页面的成功、空、加载、错误和降级状态；
5. 与现有 Flutter 页面对应的组件拆分建议。

如果你的代码输出目标是 React/Next，请把它当作视觉原型，不要删除或重命名现有 Flutter 的业务契约；同时标注每个组件在 Flutter 中应映射到的 Widget。所有中文文案保持自然、简洁、克制，优先交付可直接评审的移动端界面。
```
